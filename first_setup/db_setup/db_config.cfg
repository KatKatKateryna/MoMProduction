# =============================================================================
# db_config.cfg
# Database configuration for setup_postgres.sh
# Edit this file to update credentials or settings, then re-run the script.
# =============================================================================

DB_NAME="postgres"                  # default PostgreSQL database
ADMIN_USER="mom_admin"              # restricted app user (remote access)
ADMIN_PASSWORD="???"		        # remote app user password
SUPERUSER="postgres"                # local-only superuser (Ubuntu default, do not change)
SUPERUSER_PASSWORD="???"	        # local superuser password
DATA_DIR="/root/postgres/data"      # where PostgreSQL stores its data files
